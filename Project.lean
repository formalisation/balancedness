-- Root of the `Project` library. Import every module so `lake build` (the oracle)
-- checks the whole formalization. Rename `Project` to your project name.
import Project.Basic
import Project.GroupAction
import Project.MomentMap
import Project.KempfNess
import Project.Main
